package chiskien.datastructures_algorithms.searching;

//Find the Kth largest Element
public class KthLargestElement {

    //Brute-force: sort the input and return the element at index k -1
    //Time complexity: O(nlogn) (because using quicksort)

    //Related Problem: heap/KClosetStar.java, hashmap/KMostFrequenciesElement
    public int findTheKthLargest(int[] nums, int k) {
        return 1;
    }
}
