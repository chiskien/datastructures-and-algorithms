package chiskien.datastructures_algorithms.searching;

public class SearchInRotatedSortedArray {


    //An array is said to be cyclically sorted if it possible to cyclically shift its entries so that it becomes sorted

    //Design an algorithm with time complexity O(logn) for finding the position of the smallest element in a rotated sorted array. Assume all elements are distinct

    public int findSmallest(int[] nums) {
        return -1;
    }
}
